let source=document.querySelector("h2");
console.dir(source.innerText);

source.innerText=source.innerText+"  "+"Rock is cooking ";



let divs=document.querySelectorAll(".box");
console.dir(divs);
 
for(div of divs){
  console.dir(divs);

  
}